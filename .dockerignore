node_modules
.next
.tmp
.codegraph
coverage
playwright-report
test-results
audit
**/.DS_Store
._*
**/._*
.commandcode
.env*
*.md
Registros SG-SST
# Seed data lives in markdown and must reach the build context despite *.md above.
!db/seed/trabajadores_por_faena_actualizado.md
.git
.github
# Local documentation, virtual environments and generated evidence are not
# inputs to the production image. Keeping them out avoids sending gigabytes of
# unrelated data to the Docker daemon for every deploy build.
docs/
# Fixtures de prueba: el libro PDTP definitivo pesa 4,5 MB y sólo lo leen los
# tests. Vivía en la raíz y también viajaba al contexto de build; al restaurarlo
# bajo tests/ hay que dejarlo fuera explícitamente o vuelve el mismo peso por
# cada deploy.
tests/fixtures/
.venv/
.vitest-reports/
artifacts/
storage/
*.tsbuildinfo
/beaconhs-main

/plataforma-portalchome-vulnerabilidades

/.github
/chome_conciliador_odoo_erpnext_pack
/qa
/erpnext-develop
/odoo-19.0
/docs
auditoria/
docs/generado