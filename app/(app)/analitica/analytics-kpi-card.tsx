// Re-export delgado: KpiCard ahora vive en components/ui/kpi-card.tsx (system surface).
// Conservado para no romper los imports existentes desde la feature analitica/combustibles.
export { KpiCard } from "@/components/ui/kpi-card"
